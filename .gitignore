node_modules
dist
.env
.env.*
!.env.example

# Generated by the router plugin from src/routes/ — regenerated on every dev server
# start and every build. Never commit it.
src/routeTree.gen.ts

# Written into the project root by the preview environment every time the dev server
# starts — not part of the app.
vite.sprites-preview.config.ts
